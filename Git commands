git init                        to initiate
git status                      to check status
git add .                       to add files
git commit -m "comment"         to commit
git remote add origin **url of repository       to set the url
git remote -v                   to check if url are set or not
git push origin master          to push codes
test



